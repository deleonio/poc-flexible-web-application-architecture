language: node_js
node_js:
  - 12
notifications:
  email:
    on_success: change
    on_failure: always
install:
  - yarn
jobs:
  include:
    - script:
        - yarn test
        - yarn coverage
        - yarn coveralls
        - yarn build-all
