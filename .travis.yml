language: node_js
node_js:
  - 12
cache:
  directories:
    - node_modules
  files:
    - yarn.lock
notifications:
  email:
    on_success: change
    on_failure: always
install:
  - yarn
jobs:
  include:
    - script:
        - yarn coverage
        - yarn coveralls
        - yarn build-all
